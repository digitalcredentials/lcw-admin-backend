{
  "name": "lcw-admin-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Build root for the bundled handlers: holds the deps they share and the code in src/shared",
  "type": "module",
  "engines": {
    "node": ">=24.0"
  },
  "dependencies": {
    "@interop/http-digest-header": "^3.0.1"
  }
}
